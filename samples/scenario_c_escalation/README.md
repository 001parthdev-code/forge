# Scenario C — Human Escalation

Synthetic command-injection case whose command structure exceeds the v0 deterministic planner's supported source pattern. Secure SWE must not fabricate a repair; it should stop safely with `HUMAN_REVIEW_REQUIRED`.
