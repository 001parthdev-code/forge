[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "secure-swe"
version = "0.1.0"
description = "Secure SWE Agent — IBM Bob 2.0 Hackathon"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
